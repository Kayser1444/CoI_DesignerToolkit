{
  "last_blueprint_folder": {
    "default": "",
    "description": "Do not delete — stores the last-open blueprint book folder so the mod can restore it on load."
  },
  "markdown_table_language": {
    "default": 0,
    "is_integer": true,
    "description": "Initial Markdown table language for saves without Designer Toolkit settings state (0=English, 1=Local, 2=Both, 3=Hybrid).",
    "min": 0,
    "max": 3
  },
  "markdown_number_format": {
    "default": 0,
    "is_integer": true,
    "description": "Initial Markdown number format for saves without Designer Toolkit settings state (0=Auto, 1=English/en-US separators, 2=Local/current-language separators).",
    "min": 0,
    "max": 2
  },
  "instant_build_mode": {
    "default": false,
    "description": "Initial instant build mode for saves without BDT settings state. When enabled, BDT completes construction and deconstruction automatically."
  },
  "legacy_belt_configurations": {
    "default": true,
    "description": "Initial state for legacy belt configurations (Update 1 style transport construction that allows turning and inclining/declining on the same tile)."
  },
  "transport_cleanup_hotkey_primary": {
    "default": "LeftAlt + Delete",
    "description": "Initial primary transport cleanup hotkey for saves without BDT settings state. Leave empty to disable the primary binding."
  },
  "transport_cleanup_hotkey_secondary": {
    "default": "",
    "description": "Initial secondary transport cleanup hotkey for saves without BDT settings state. Leave empty to disable the secondary binding."
  },
  "height_filter_show_layer_hotkey_primary": {
    "default": "PageUp",
    "description": "Initial primary height filter show layer hotkey. Leave empty to disable the primary binding."
  },
  "height_filter_show_layer_hotkey_secondary": {
    "default": "",
    "description": "Initial secondary height filter show layer hotkey. Leave empty to disable the secondary binding."
  },
  "height_filter_hide_layer_hotkey_primary": {
    "default": "PageDown",
    "description": "Initial primary height filter hide layer hotkey. Leave empty to disable the primary binding."
  },
  "height_filter_hide_layer_hotkey_secondary": {
    "default": "",
    "description": "Initial secondary height filter hide layer hotkey. Leave empty to disable the secondary binding."
  },
  "dtkSettingsStateJson": {
    "default": "{\"schemaVersion\":1}",
    "description": "Internal per-save Designer Toolkit settings state. Editing may reset saved BDT settings."
  },
  "bdtRateLimitsStateJson": {
    "default": "{\"schemaVersion\":1}",
    "description": "Internal per-save Designer Toolkit rate limits state. Editing may reset saved BDT rate limits."
  }
}

