{
  "startNewGamePaused": {
    "default": true,
    "description": "When starting a new game, automatically pause simulation at tick 0."
  },
  "saveGameOnFirstDayAs": {
    "default": "Initial",
    "description": "When starting a new game, create an initial autosave under this name when first unpaused (blank disables)."
  },
  "blueprint_spacing": {
    "default": 6,
    "is_integer": true,
    "description": "Initial blueprint spacing parameter [0-12] for batch pasting.",
    "min": 0,
    "max": 12
  },
  "markdown_table_language": {
    "default": 0,
    "is_integer": true,
    "description": "Initial Markdown table language for saves without Designer Toolkit settings state (0=English, 1=Local, 2=Both, 3=Hybrid).",
    "min": 0,
    "max": 3
  },
  "markdown_number_format": {
    "default": 0,
    "is_integer": true,
    "description": "Initial Markdown number format for saves without Designer Toolkit settings state (0=Auto, 1=English/en-US separators, 2=Local/current-language separators).",
    "min": 0,
    "max": 2
  },
  "instant_build_mode": {
    "default": false,
    "description": "Initial instant build mode for saves without BDT settings state. When enabled, BDT completes construction and deconstruction automatically."
  },
  "legacy_belt_configurations": {
    "default": true,
    "description": "Initial state for legacy belt configurations (Update 1 style transport construction that allows turning and inclining/declining on the same tile)."
  },
  "pre_color_pipes": {
    "default": true,
    "description": "Initial state for coloring empty pipes from their connected upstream sources."
  },
  "height_filter_transport_visibility": {
    "default": 1,
    "is_integer": true,
    "description": "Initial transport visibility mode for height filtering for saves without BDT settings state (0=Low, 1=Medium, 2=High).",
    "min": 0,
    "max": 2
  },
  "height_filter_pillar_visibility": {
    "default": 0,
    "is_integer": true,
    "description": "Initial pillar visibility mode for height filtering for saves without BDT settings state (0=Detached, 1=Attached, 2=Top, 3=Off).",
    "min": 0,
    "max": 3
  },
  "throughput_overlay_enabled": {
    "default": false,
    "description": "Initial throughput overlay visibility state for saves without BDT settings state. When enabled, configured throughput overlays will render in-world."
  },
  "throughput_glow_enabled": {
    "default": false,
    "description": "Initial throughput glow visibility state for saves without BDT settings state."
  },
  "throughput_heatmap_mode": {
    "default": 2,
    "is_integer": true,
    "description": "Initial throughput heatmap mode for saves without BDT settings state (0=None, 1=Relative, 2=Capacity).",
    "min": 0,
    "max": 2
  },
  "throughput_colorblind_mode": {
    "default": false,
    "description": "Initial throughput colorblind-friendly colors mode for saves without BDT settings state."
  },
  "throughput_show_as_percent": {
    "default": false,
    "description": "Initial throughput show as percent mode for saves without BDT settings state."
  },
  "layout_box_mode_enabled": {
    "default": false,
    "description": "Initial layout box mode visibility state for saves without BDT settings state. When enabled, building occupancy hitboxes will render in 3D."
  },
  "use_recycle_bin": {
    "default": true,
    "description": "Initial state for using the blueprint recycle bin. When enabled, deleted or updated blueprints are copied to the recycle bin folder."
  },
  "recycle_bin_folder_name": {
    "default": "Recycle Bin",
    "description": "Initial folder name for the blueprint recycle bin."
  },
  "pollution_overlay_enabled": {
    "default": false,
    "description": "Initial pollution overlay visibility state. When enabled, configured pollution overlays will render in-world."
  },
  "pollution_glow_enabled": {
    "default": false,
    "description": "Initial pollution glow visibility state."
  },
  "pollution_glow_color": {
    "default": "white",
    "description": "Initial pollution heatmap glow color. Use white, brown, purple, or #RRGGBB."
  },
  "pollution_days_to_average": {
    "default": 360,
    "is_integer": true,
    "description": "Initial pollution days to average (0-360, 0 disables data collection)."
  },
  "pollution_show_air": {
    "default": true,
    "description": "Initial state for showing air pollution."
  },
  "pollution_show_ground": {
    "default": true,
    "description": "Initial state for showing ground and water pollution."
  },
  "pollution_show_solid_waste": {
    "default": true,
    "description": "Initial state for showing ground pollution from landfill."
  },
  "pollution_show_vehicle": {
    "default": true,
    "description": "Initial state for showing vehicle and train pollution."
  },
  "pollution_show_ship": {
    "default": true,
    "description": "Initial state for showing ship pollution."
  },
  "radiation_overlay_enabled": {
    "default": false,
    "description": "Initial radiation overlay visibility state. When enabled, sampled unsafe radioactive inventory will render in-world."
  },
  "radiation_glow_enabled": {
    "default": false,
    "description": "Initial radiation glow visibility state."
  },
  "radiation_days_to_average": {
    "default": 30,
    "is_integer": true,
    "description": "Initial radiation days to average (0-360, 0 disables data collection)."
  },
  "dtkSettingsStateJson": {
    "default": "{\"schemaVersion\":1}",
    "description": "Internal per-save Designer Toolkit settings state. Editing may reset saved BDT settings."
  },
  "bdtRateLimitsStateJson": {
    "default": "{\"schemaVersion\":1}",
    "description": "Internal per-save Designer Toolkit rate limits state. Editing may reset saved BDT rate limits."
  },
  "bdtThroughputConfigStateJson": {
    "default": "{\"schemaVersion\":1}",
    "description": "Internal per-save Designer Toolkit throughput configurations state. Editing may reset saved BDT throughput configs."
  },
  "bdtTransportRemovalOrdersStateJson": {
    "default": "{\"schemaVersion\":1,\"orders\":[]}",
    "description": "Internal per-save Designer Toolkit transport product-removal order state. Editing may reset active removal orders."
  },
  "bdtLegacyStackerFullAlertStateJson": {
    "default": "{\"schemaVersion\":1,\"disabledEntityIds\":[]}",
    "description": "Internal per-save Designer Toolkit legacy stacker full-alert state. Editing may reset per-stacker alert preferences."
  },
  "last_blueprint_folder": {
    "default": "",
    "description": "Do not delete — stores the last-open blueprint book folder so the mod can restore it on load."
  }
}
